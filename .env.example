# Supabase public client configuration
# These values are safe to expose in the browser when Row Level Security is configured.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Product environment
VITE_APP_ENV=local
